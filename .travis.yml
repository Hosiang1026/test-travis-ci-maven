language: java

jdk:
  - oraclejdk8

cache:
  directories:
  - '$HOME/.m2/repository'
  
#before_install:
# - chmod +x gradlew

script:
  - mvn clean package -DskipTests=true
  
after_success:
  - cd ./target
  - git init
  - git config user.name "Hosiang1026"
  - git config user.email "hosiang969@sina.com"
  - git add spring-boot-jsp.war
  - git commit -m "version 1"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master

branches:
  only:
    - test

notifications:
  email: false
  
env:
  global:
  - GH_REF=github.com/Hosiang1026/test-travis-ci-maven.git
